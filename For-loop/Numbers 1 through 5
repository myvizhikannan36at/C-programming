#include <stdio.h>

int main() {
    // 1. i = 1 (Start)
    // 2. i <= 5 (Check condition)
    // 3. i++ (Step up by 1 after each loop)
    for (int i = 1; i <= 5; i++) {
        printf("Iteration: %d\n", i);
    }

    return 0;
}
