#include <stdio.h>

int main() {
    char name[] = "Gemini"; 

    for (int i = 1; i <= 5; i++) {
        // Printing the iteration number and the name
        printf("%d. Hello, %s!\n", i, name);
    }

    return 0;
}
